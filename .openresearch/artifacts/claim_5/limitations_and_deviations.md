# Claim 5 limitations and deviations

- No physical lensing inference is run because the exact raw posterior inputs
  and essential generative/sampler details are absent. Running a guessed
  implementation for 13.33 CPU-hours would be a different experiment, not a
  faithful reproduction.
- The pinned notebook's aggregate values establish what the authors plotted,
  but do not constitute independent evidence for the claim.
- `BLOCKED` is a terminal evidence verdict about reproducibility from the
  available record. It is neither `VERIFIED` nor `FALSIFIED`.
- The existing 100-dimensional GMM experiment remains preserved as historical
  evidence but is explicitly excluded from this claim contract.
